---
title: "pkgreporting"
author: "Mara Averick"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r include=FALSE}
suppressPackageStartupMessages(library(tidyverse))
library(DT)
```

```{r load_iss, include=FALSE}
issues <- readRDS(here::here("data", "issues.Rds"))
```

```{r select_iss, include=FALSE}
issues_pared <- issues %>%
  select(c("repo", "title", "comments", "bug", "reaction_p1", "reaction_other"))
```


```{r dt, echo=FALSE}
datatable(issues_pared, options = list(pageLength = 20)) %>%
  formatStyle('reaction_p1',  color = 'black', fontWeight = 'bold')
```


